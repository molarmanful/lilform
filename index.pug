doctype html
html
  head
    title Env Test
  body
    pre#box
    script(src='env.js')
    script.
      ENV = new Env(50,50)

      ;[...new Array(10)].map(_=> ENV.addCell())
      ;[...new Array(7000)].map(_=> ENV.addFood())

      setInterval(_=>{
        box.innerHTML = ENV+''
        ENV.step()
      },500)
